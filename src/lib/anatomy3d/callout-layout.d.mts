type Region={id:string;x:number;y:number;width:number;height:number};
export function layoutCalloutBadges(regions:Region[],width:number,height:number):{id:string;x:number;y:number;size:number;anchorX:number;anchorY:number}[];
